function fish_user_key_bindings
    # Edit the complete current command in Neovim.
    bind \ee edit_command_buffer
    bind \ev edit_command_buffer

    # Ctrl+X followed by Ctrl+E.
    bind \cx\ce edit_command_buffer
end
